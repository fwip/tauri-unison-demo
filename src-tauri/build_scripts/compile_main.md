This transcript will download and compile the project.

(Edit `src-tauri/Cargo.toml` to change what values you're using)

```ucm
.> clone @dfreeman/tvGuide
@dfreeman/tvGuide/main> compile wordle.serve main
```
